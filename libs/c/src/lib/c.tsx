import React from 'react';

import './c.css';

/* eslint-disable-next-line */
export interface CProps {}

export const C = (props: CProps) => {
  return (
    <div>
      <h1>Welcome to c!</h1>
    </div>
  );
};

export default C;
