import React from 'react';

import './a.css';

/* eslint-disable-next-line */
export interface AProps {}

export const A = (props: AProps) => {
  return (
    <div>
      <h1>Welcome to a!</h1>
    </div>
  );
};

export default A;
